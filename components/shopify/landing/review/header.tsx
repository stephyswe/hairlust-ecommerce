export const ReviewHeader = ({ title }: any) => (
  <header className="references__header references__header--light container">
    <h2 className="references__header__title title">{title}</h2>
  </header>
)
